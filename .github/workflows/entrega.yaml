name: Olá Mundo

on: push

jobs:
  ola:
    runs-on: ubuntu-latest
    env:
      MINHA_OUTRA_VAR: "outra coisa"
    steps:
      - name: Dizer ola
        run: echo " Olá Impacta a minha variável do repositório é ${{ vars.MINHA_VAR }}"

      - name: Dizer outra coisa
        run: echo "Agora estou $MINHA_OUTRA_VAR"
  